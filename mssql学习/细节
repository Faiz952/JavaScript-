like关键字：like string:
    1、string里的 `'`，需要转 `''`，
    2、如果想转义 `%` 或者 `_`，需要在 like string 后面添加一句 {escape '\'}，
       '\'表示转义字符，将后面的一个字符转义，所以，转义`%`或者`_`需要将%替换成`\%`或`\_`
mssql的外键：必须是主键或候选键，外键字段钟任一字段若为null，将不与父表做关联



